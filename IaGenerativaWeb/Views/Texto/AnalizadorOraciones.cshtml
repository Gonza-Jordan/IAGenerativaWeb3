@model IAGenerativaDemo.Web.Models.TextoViewModel
@{
    ViewData["Title"] = "Analizador de Oraciones";
}
<div class="container" style="max-width: 1000px;">
    <h2 class="text title mb-4 text-center">Analizador de Oraciones</h2>
    <form asp-action="AnalizadorOraciones" method="post" class="form shadow-lg rounded p-4 mb-4" style="background:rgba(30,30,50,0.97);">
        <label for="texto" class="text subtitle mb-2">Ingrese la oración a analizar:</label>
        <textarea id="texto" name="Texto" rows="4" cols="60" class="form-control mb-3 textarea" style="background:#f5e6fa;">@Model?.Texto</textarea>
        <button type="submit" class="btn btn-primary w-100 mt-2" style="font-weight:bold;">Analizar</button>
</form>
@if (!string.IsNullOrEmpty(Model?.Texto))
{
    <hr />
    <h4>Texto ingresado:</h4>
    <div>@Model.Texto</div>
}


<div class="row justify-content-center mb-5">
    <div class="col-12 mb-3 text-center">
        <h4 class="sabias-que text-center" style="color: #fff; text-shadow: 0 2px 10px #7f39fb, 0 1px 2px #000;">
            ¿Sabías que...?
        </h4>
    </div>
    <div class="col-12 col-md-6 mb-3">
        <div class="card h-100 shadow-sm border-0" style="background:#e1ecf7;">
            <div class="card-body d-flex flex-column justify-content-center align-items-center">
                <div style="font-size:2.1rem;">💼</div>
                <h6 class="card-title mt-2 mb-2" style="font-weight: bold; color: #215676;">El 80% de los mails laborales usan tono formal</h6>
                <p class="card-text mb-0 text-center" style="color:#20425a;">
                    En ambientes de trabajo, más del <strong>80% de los correos electrónicos</strong> y mensajes utilizan frases formales para evitar malentendidos y transmitir profesionalismo.
                    <br>
                    <span style="font-size:0.89rem; color:#4879a6;">(Fuente: Radicati Group Email Statistics, 2023)</span>
                </p>
            </div>
        </div>
    </div>
    <div class="col-12 col-md-6 mb-3">
        <div class="card h-100 shadow-sm border-0" style="background:#f5e6fa;">
            <div class="card-body d-flex flex-column justify-content-center align-items-center">
                <div style="font-size:2.1rem;">📱</div>
                <h6 class="card-title mt-2 mb-2" style="font-weight: bold; color: #813d9c;">Las frases informales son clave en redes</h6>
                <p class="card-text mb-0 text-center" style="color:#76459a;">
                    En redes sociales y chats, las frases informales <strong>generan 3 veces más interacción</strong> que las formales, ayudando a construir cercanía y confianza con la audiencia.
                    <br>
                    <span style="font-size:0.89rem; color:#a074ba;">(Fuente: Sprout Social Index, 2023)</span>
                </p>
            </div>
        </div>
    </div>
</div>
</div>
<a href="@Url.Action("Index", "Home")" class="back">Volver</a>


<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
<link rel="stylesheet" href="~/css/index.css" />
<script src="~/js/validacionTexto.js"></script>