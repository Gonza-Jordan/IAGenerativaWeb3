@model IAGenerativaDemo.Web.Models.TextoViewModel
@{
    ViewData["Title"] = "Transformar Texto";
}

<div class="container" style="max-width: 1000px;">
    <h2 class="text title mb-4 text-center">Transformador de Textos</h2>
    <form asp-action="TransformadorTextos" method="post" class="form shadow-lg rounded p-4 mb-4" style="background:rgba(30,30,50,0.97);">
        <label for="Texto" class="text subtitle mb-2">Texto a transformar:</label>
        <textarea name="Texto" id="Texto" rows="3" class="form-control mb-3 textarea" style="background:#f5e6fa;">@Model?.Texto</textarea>
        @if (ViewData.ModelState["Texto"]?.Errors.Count > 0)
        {
            foreach (var error in ViewData.ModelState["Texto"].Errors)
            {
                <div class="alert alert-danger mt-2">@error.ErrorMessage</div>
            }
        }
        <label class="text subtitle mb-2">Transformar a:</label>
        <div class="d-flex gap-3 mb-3">
            <div>
                <input type="radio" id="Formal" name="OpcionTransformar" value="Formal" @(Model?.OpcionTransformar == "Formal" ? "checked" : "") />
                <label for="Formal" style="margin-left:4px;">Formal</label>
            </div>
            <div>
                <input type="radio" id="Informal" name="OpcionTransformar" value="Informal" @(Model?.OpcionTransformar == "Informal" ? "checked" : "") />
                <label for="Informal" style="margin-left:4px;">Informal</label>
            </div>
        </div>
        @if (ViewData.ModelState["OpcionTransformar"]?.Errors.Count > 0)
        {
            foreach (var error in ViewData.ModelState["OpcionTransformar"].Errors)
            {
                <div class="alert alert-danger mt-2">@error.ErrorMessage</div>
            }
        }
        <button type="submit" class="btn btn-success w-100 mt-2" style="font-weight:bold;">Transformar</button>
    </form>

    @if (!string.IsNullOrEmpty(Model?.TextoTransformado))
    {
        <div class="card shadow-sm mb-4 border-0" style="background: #eaf6ea;">
            <div class="card-body">
                <h5 class="mb-2 text-success" style="font-weight:bold;">Texto ingresado:</h5>
                <div style="font-size:1.08rem; color: #235e42;">@Model.Texto</div>
                <hr />
                <h5 class="mb-2 text-primary" style="font-weight:bold;">Texto transformado:</h5>
                <div style="font-size:1.14rem; color: #425793;">@Model.TextoTransformado</div>
            </div>
        </div>
    }

    <div class="row justify-content-center mb-5">
        <div class="col-12 mb-3 text-center">
            <h4 class="sabias-que text-center" style="color: #fff; text-shadow: 0 2px 10px #7f39fb, 0 1px 2px #000;">
                ¿Sabías que...?
            </h4>
        </div>
        <div class="col-12 col-md-6 mb-3">
            <div class="card h-100 shadow-sm border-0" style="background:#fff2cc;">
                <div class="card-body d-flex flex-column justify-content-center align-items-center">
                    <div style="font-size:2.1rem;">🗣️</div>
                    <h6 class="card-title mt-2 mb-2" style="font-weight: bold; color: #795548;">Adaptar el tono aumenta la comprensión</h6>
                    <p class="card-text mb-0 text-center" style="color:#674106;">
                        <strong>El 60% de los profesionales</strong> afirma que adaptar el tono del mensaje al público objetivo facilita que el mensaje sea comprendido y recordado.<br>
                        <span style="font-size:0.89rem; color:#967329;">(Fuente: Harvard Business Review, 2023)</span>
                    </p>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-6 mb-3">
            <div class="card h-100 shadow-sm border-0" style="background:#e1f7ec;">
                <div class="card-body d-flex flex-column justify-content-center align-items-center">
                    <div style="font-size:2.1rem;">🔄</div>
                    <h6 class="card-title mt-2 mb-2" style="font-weight: bold; color: #186b4a;">Formal o informal: ¡el contexto importa!</h6>
                    <p class="card-text mb-0 text-center" style="color:#1a6146;">
                        Cambiar de un estilo formal a uno informal <strong>puede mejorar la conexión y confianza</strong> con el destinatario, especialmente en comunicaciones digitales y equipos ágiles.<br>
                        <span style="font-size:0.89rem; color:#257156;">(Fuente: Sprout Social Index, 2023)</span>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<button type="button" class="back" style="margin-bottom: 2rem;" onclick="location.href='@Url.Action("Index", "Home")'">Ir al Inicio</button>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
<link rel="stylesheet" href="~/css/index.css" />
<script src="~/js/validacionTexto.js"></script>
